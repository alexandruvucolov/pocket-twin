fastapi==0.115.12
uvicorn[standard]==0.34.0
aiortc==1.10.1
python-dotenv==1.0.1
pydantic==2.10.6
numpy>=1.23.5,<2
opencv-python-headless==4.11.0.86
av==13.1.0
mediapipe==0.10.21
httpx>=0.27.0
# MuseTalk deps — installed in venv so they shadow the incompatible system versions.
# torch/torchvision are NOT installed in venv; they are found via sys.path.append
# on the system site-packages (torch 2.4.1+cu124 / torchvision 0.19.1+cu124).
# Installing torch in both venv AND system produces two torch.Tensor class objects
# in the same process, which breaks diffusers' infer_schema op registration.
huggingface-hub>=1.0.0
transformers>=4.48.0
diffusers>=0.30.0
accelerate>=0.34.0
